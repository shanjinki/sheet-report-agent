# Report Template Pack

This pack gives agents a compact library of report directions. It is intentionally lighter than a folder full of complete HTML files: agents read `selection-index.json`, shortlist candidates, then read only the relevant `preview.md` cards.

Each preview defines:
