# Template Notes

The generator uses built-in HTML templates in `scripts/generate_report.py` so it can run from a single checkout without a build step. This folder is reserved for future swappable templates, screenshots, or brand assets.
